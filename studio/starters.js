/* ============================================================
   Newtuple Brand Studio — CUSTOM STARTER TEMPLATES
   ------------------------------------------------------------
   Add your own reusable, editable templates here. They appear in
   the Library → "Starter collection" alongside the built-ins, and
   can be opened, edited, repurposed and restyled like any asset.

   This file is safe to hand-edit (unlike references.js, which is
   generated). Each entry is plain data:

     { name, kind, theme, data }

   - kind:  'carousel' | 'post' | 'banner' | 'onepager' | 'proposal'
   - theme: 'light' | 'dark'   (carousels/posts/banners support both;
            onepager & proposal are 'light')
   - data:  the same shape the Studio uses for that kind. The easiest
            way to author one: build it in the Studio, click
            "★ Save current", then Export the library JSON and copy
            the entry's `data` here. Wrap a phrase in **stars** for
            cobalt/cyan emphasis.
   ============================================================ */

window.NT_STARTERS = [

  // ---- Example (delete or edit) -------------------------------
  // {
  //   name: 'Production-grade AI (banner)',
  //   kind: 'banner',
  //   theme: 'dark',
  //   data: {
  //     kicker: 'Production-grade AI',
  //     title: 'We get AI to production at **warp speed**.',
  //     subtitle: 'Past the proof-of-concept — deployed with SLAs, observability and continuous optimization.'
  //   }
  // },

];
